Question:-
Task

You are given a string .
Your task is to print all possible permutations of size  of the string in lexicographic sorted order.

Solution:-
from itertools import permutations

s, k = input().split()

result = sorted(permutations(s, int(k)))

for item in result:
    print(''.join(item))
